{
  "name": "@chin0102/mp-adapter",
  "version": "0.1.0",
  "description": "Adapters for Node.js and WeChat Mini Program projects.",
  "type": "module",
  "main": "./index.cjs",
  "module": "./src/index.js",
  "exports": {
    ".": {
      "import": "./src/index.js",
      "require": "./index.cjs",
      "default": "./index.cjs"
    }
  },
  "files": [
    "src",
    "index.cjs"
  ],
  "sideEffects": false,
  "scripts": {
    "dev:push": "npm run build && yalc push",
    "build": "esbuild src/index.js --bundle --format=cjs --platform=neutral --target=es2018 --outfile=index.cjs",
    "check": "npm run build && node --check src/index.js && node --check index.cjs",
    "test": "node --test",
    "prepack": "npm run build",
    "prepublishOnly": "npm run check"
  },
  "keywords": [
    "wechat",
    "miniprogram",
    "adapter",
    "esm"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "prettier": "3.9.5"
  }
}
